# METCS777-TestPaper

Each file is named according to the file format(CSV or Parquet) and Cloud Service the code was run on(AWS or Google Cloud).
The taxi dataset we have used for former assignments is the dataset we chose to use. We chose three tasks from those assignments.
Task 1 is the best hour to work as a taxi, Task 2 is top 10 best drivers, and Task 3 is the top 10 taxis with the most drivers. 
